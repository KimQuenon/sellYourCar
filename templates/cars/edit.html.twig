{% extends "base.html.twig" %}


{% block title %}Mode Edition{% endblock %}

{% form_theme myForm 'cars/_collection.html.twig' %}

{% block body %}

    <div class="container mt-4">
        <h1>Modification de l'annonce : {{car.fullBrand}}</strong></h1>

        {{form_start(myForm)}}

            {{form_widget(myForm)}}
            <button type="submit" class="btn btn-warning mt-3">Modifier</button>


        {{form_end(myForm)}}
    </div>

{% endblock %}

{% block javascripts %}
    {{encore_entry_script_tags('car')}}
{% endblock %}